import {HistoryItem} from './history-item';

describe('HistoryItem', () => {
  it('should create an instance', () => {
    const historyItem: HistoryItem = {} as HistoryItem;
    expect(historyItem).toBeTruthy();
  });
});
