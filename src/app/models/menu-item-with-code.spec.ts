import {MenuItemWithCode} from './menu-item-with-code';

describe('MenuItemWithCode', () => {
  it('should create an instance', () => {
    const menuItemWithCode = {} as MenuItemWithCode;
    expect(menuItemWithCode).toBeTruthy();
  });
});
